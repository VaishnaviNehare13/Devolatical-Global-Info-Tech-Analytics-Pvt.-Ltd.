/**
 * Reusable Password System Constants
 *
 * Centralizes password-related constants across the application to avoid magic numbers.
 */

export const PASSWORD_MIN_LENGTH = 8;
export const PASSWORD_MAX_LENGTH = 72; // Bcrypt maximum input bytes limit
